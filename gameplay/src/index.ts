import { euclideanDistance } from "./helper";

const point1 = { x: 10, y: 20, z: 30 };
const point2 = { x: 40, y: 50, z: 60 };

console.log(euclideanDistance(point1, point2)); // Outputs the Euclidean distance
